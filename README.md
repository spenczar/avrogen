# TODO
 TODO: Avro supports "`other_props`" - maybe this means other properties in the avsc.

 We could manage imports of referenced fields by saying "if a `python_package` field is specified, use that as the module; otherwise, use the namespace."
